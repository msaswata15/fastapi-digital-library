# fastapi-digital-library
A RESTful Digital Library API built using FastAPI as part of the AMI FastAPI Assignment. The project demonstrates core backend concepts including CRUD operations, auto-increment IDs, request validation, and proper HTTP error handling using an in-memory data store.
